NODE_ENV={{ node_env }}
PORT={{ item.port }}
DB_HOST=localhost
DB_PORT={{ item.db_port }}
DB_NAME={{ item.db }}
DB_USER={{ pg_user }}
DB_PASSWORD={{ pg_password }}
JWT_SECRET={{ jwt_secret }}

{% if item.name == 'ms-auth-service' %}
JWT_ACCESS_EXPIRY={{ jwt_access_expiry }}
JWT_REFRESH_EXPIRY={{ jwt_refresh_expiry }}
{% endif %}

{% if item.name == 'vehiculos-service' %}
PERSONAS_SERVICE_URL=http://localhost:8002
{% endif %}

{% if item.name == 'comparendos-service' %}
PERSONAS_SERVICE_URL=http://localhost:8002
VEHICULOS_SERVICE_URL=http://localhost:8003
INFRACCIONES_SERVICE_URL=http://localhost:8004
{% endif %}
