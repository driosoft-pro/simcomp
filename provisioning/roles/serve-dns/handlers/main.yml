---
# Handlers for serve-dns
- name: restart bind9
  ansible.builtin.systemd:
    name: named
    state: restarted

- name: reload bind9
  ansible.builtin.systemd:
    name: named
    state: reloaded
